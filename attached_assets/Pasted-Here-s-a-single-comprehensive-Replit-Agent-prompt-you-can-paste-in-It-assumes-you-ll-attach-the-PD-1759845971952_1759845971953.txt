Here’s a single, comprehensive Replit Agent prompt you can paste in. It assumes you’ll attach the PDF of the unified wireframes.

⸻

Replit Agent — Implement ScriptTok UI per Attached Wireframes

Context: See attached PDF: “ScriptTok – Unified Wireframes (Figma-Style)”. Implement the UI exactly as specified for:
	•	Landing Page
	•	Pricing Page
	•	Viral Content Studio
	•	Trend Dashboard (Desktop & Mobile)
	•	(Mirror style for Affiliate Content Studio)

Global UI Refactor (apply first)

Refactor the UI for ScriptTok to match a modern, clean SaaS aesthetic.
	•	Use shadcn/ui components consistently (Cards, Tabs, Inputs, Buttons)
	•	Use Tailwind utility classes for spacing and readability
	•	Keep all main content in a centered max-w-3xl container
	•	Add subtle hover transitions and rounded-xl corners
	•	Simplify any nested divs or redundant wrappers
	•	Ensure mobile responsiveness (stack elements vertically on small screens)
	•	Apply a neutral gray background with white cards
	•	Test every interactive element for consistent spacing

Tech & Conventions
	•	Stack: React + Vite + TypeScript + Tailwind + shadcn/ui (Radix primitives).
	•	Directory:

client/src/
  pages/
    Landing.tsx
    Pricing.tsx
    ViralStudio.tsx
    AffiliateStudio.tsx
    Trends.tsx
  components/
    layout/Header.tsx
    layout/Footer.tsx
    layout/Container.tsx
    ui/TrendCard.tsx
    ui/FeaturedTrend.tsx
    ui/ScoreBreakdown.tsx
    ui/TemplatePicker.tsx
    ui/OutputCard.tsx
    ui/PlanCard.tsx
    ui/StickyCTA.tsx
  lib/
    theme.ts
    routes.ts


	•	Tailwind tokens (add to theme):
	•	Base bg: bg-[#f9fafb]
	•	Card: bg-white rounded-2xl shadow-sm
	•	Accent: #2563eb
	•	Standard container: max-w-5xl mx-auto p-6 (pages), max-w-3xl (content columns)

⸻

Pages to Build

1) Landing Page (pages/Landing.tsx)
	•	Hero: headline, subtext, [Start Free] + [Watch Demo].
	•	Key Features: 2-column icon grid.
	•	Mockup Carousel: Viral Studio, Affiliate Studio, Score Breakdown.
	•	Pricing Teaser: Free vs Pro preview.
	•	Testimonials: 3 cards.
	•	CTA Footer.

Acceptance Criteria
	•	Hero text clamps on mobile, buttons wrap.
	•	Carousel is keyboard-accessible, swipeable on mobile.

⸻

2) Pricing Page (pages/Pricing.tsx)
	•	3 Plan Cards (Free / Pro / Creator+). Pro is “Most Popular”.
	•	Monthly/Yearly toggle.
	•	Optional comparison table.
	•	FAQ (Accordion).
	•	Final CTA banner.

Acceptance Criteria
	•	Toggle updates prices without layout shift.
	•	Cards equal height; focus/hover states consistent.

⸻

3) Viral Content Studio (pages/ViralStudio.tsx)
	•	Trend Summary Card (optional).
	•	Prompt Input: textarea + Template Select + Tone Select + [Generate].
	•	Viral Score Breakdown: Overall + bars for Hook/Engagement/Clarity/Length/Trending + suggestions.
	•	Output Cards grid: title, 3-line script, hashtags, [Copy] [View Score].
	•	Bottom Toolbar: Back to templates, History, Upgrade.

Acceptance Criteria
	•	On generate, show skeleton loaders.
	•	Score breakdown uses shadcn Progress (or custom) and tooltips.
	•	Keyboard shortcuts: Cmd/Ctrl+Enter triggers Generate.

⸻

4) Affiliate Content Studio (pages/AffiliateStudio.tsx)
	•	Use the exact same layout & styles as Viral Studio.
	•	Replace Score name if needed (e.g., “Performance Score”).
	•	Output card fields: product name, price, affiliate link button, captions/hashtags, [Copy].

Acceptance Criteria
	•	Theming, spacing, card structure match Viral Studio 1:1.
	•	Buttons and tooltips consistent.

⸻

5) Trend Dashboard (pages/Trends.tsx)

Desktop
	•	Page Header: title + [Refresh Trends] [Export Report].
	•	Sticky Filter Bar: Niche select, Trend Stage select, search input.
	•	Featured Trend card (Top Focus) with [Insights] and [Generate].
	•	Trend Category Grid with tabs: Hot | Rising | Upcoming | Avoid. Show 5–6 results then [Load More].
	•	AI-Powered Product Picks: compact card grid (3–4 per row), one-line “Why it’s hot”.
	•	Tip bar and page CTA footer → [Go to Viral Studio].

Mobile Adaptation
	•	Compact header + slide-in menu via Sheet.
	•	Collapsible filters.
	•	Featured Trend becomes hero card.
	•	Tabs scroll horizontally (swipe).
	•	Single-column stacked cards.
	•	Sticky bottom CTA [Go to Viral Studio →].
	•	AI Picks turn into a horizontal carousel with snap scrolling.

Acceptance Criteria
	•	Filters update results instantly (mock state ok).
	•	Cards equal heights; “Generate” is primary action on each card.
	•	Sticky CTA appears only on small breakpoints.

⸻

Component Specs (Build these as shadcn wrappers)
	•	Container: centers content; supports size="xl|lg|md" props.
	•	Header: brand at left, nav in middle, auth/profile at right; mobile Sheet menu.
	•	PlanCard: plan name, price area w/ monthly/yearly, features list, CTA, “Most Popular” badge.
	•	TemplatePicker: Select for template/tone; emits selected values.
	•	ScoreBreakdown: overall number + 5 metric bars + suggestions list; ARIA labels + tooltips.
	•	TrendCard: title, one-line summary, product badges w/ price, [Generate Script] [More Info].
	•	FeaturedTrend: gradient background variant with large CTA.
	•	OutputCard: script, hashtags, copy button, optional View Score link.
	•	StickyCTA: portal to bottom of viewport on mobile only.

Each component: no inline magic numbers—use Tailwind utilities and variants.

⸻

Styling Rules (apply across app)
	•	Cards: bg-white rounded-2xl shadow-sm hover:shadow transition
	•	Buttons: rounded-xl, consistent padding; primary uses accent color.
	•	Spacing: default gap-4, page sections mt-8.
	•	Typography: headings font-semibold, readable body text-gray-700.
	•	Badges for niches (soft color variants): beauty=pink, tech=blue, fitness=green, fashion=purple, food=orange, pets=slate.

⸻

Routing & Nav
	•	Add routes:
	•	/ (Landing)
	•	/pricing
	•	/studio/viral
	•	/studio/affiliate
	•	/trends
	•	Header nav highlights active route; mobile menu mirrors links.

⸻

Accessibility & QA Checklist (must pass)
	•	All interactive elements are reachable via keyboard (Tab/Shift+Tab); visible focus ring.
	•	Color contrast AA minimum.
	•	Landmarks: header, main, nav, footer.
	•	aria-labels for icons (e.g., refresh, export).
	•	Skeleton loaders for generate actions.
	•	Responsive tests: 360px, 768px, 1024px, 1280px widths.
	•	Visual regression: ensure equal card heights and consistent gaps.

⸻

Implementation Steps
	1.	Install & init shadcn/ui; generate needed components (Button, Card, Tabs, Select, Accordion, Sheet, Tooltip, Progress).
	2.	Add Tailwind theme tokens and base styles (bg, accent, radii).
	3.	Build Header, Footer, Container.
	4.	Implement Landing and Pricing pages first (static).
	5.	Implement Viral Studio (with mock “generate” handler + score mock).
	6.	Clone Affiliate Studio from Viral; replace fields; keep identical styling.
	7.	Implement Trends (desktop layout); then add mobile responsive rules & StickyCTA.
	8.	Add light E2E checks (Playwright or Cypress) for nav + responsive breakpoints.
	9.	Run a UI sweep: remove redundant wrappers, normalize spacing, verify hover/focus.

⸻

Definition of Done
	•	All five pages render with pixel-consistent layout matching the PDF wireframes.
	•	Shared styles/components ensure visual parity across Viral & Affiliate Studios.
	•	Mobile experience is thumb-friendly, with sticky CTA and swipeable tabs/carousels.
	•	No console errors; lighthouse accessibility score ≥ 90 on each page.

⸻

If anything in the PDF is ambiguous, make the most reasonable UX choice and note it in a short docs/UX_NOTES.md file.