# ============================================
# SCRIPTTOK ENVIRONMENT CONFIGURATION
# ============================================

# --------------------------------------------
# ENVIRONMENT SETTINGS
# --------------------------------------------
# Application environment: "development" or "production"
APP_ENV=development

# Application URL (change per environment)
APP_URL=http://localhost:5000

# Frontend API base URL (for Vite client)
VITE_API_BASE_URL=http://localhost:5000/api

# --------------------------------------------
# DEVELOPMENT BYPASS FLAGS
# --------------------------------------------
# Bypass usage limits in development (1=enabled, 0=disabled)
# In dev: 1 = unlimited generations
# In prod: 0 = enforce tier-based limits
BYPASS_LIMITS=1

# Disable billing integration in development (1=disabled, 0=enabled)
# In dev: 1 = mock subscription without Stripe
# In prod: 0 = enable Stripe billing
DISABLE_BILLING=1

# --------------------------------------------
# DEVELOPMENT USER (Dev Environment Only)
# --------------------------------------------
# Fixed user credentials for development auto-login
# These are only used when APP_ENV=development
DEV_USER_ID=00000000-0000-0000-0000-000000000001
DEV_USER_EMAIL=dev@scripttok.local
DEV_USER_NAME=Dev User

# --------------------------------------------
# DATABASE
# --------------------------------------------
# PostgreSQL connection string
# Use separate databases for dev and prod
DATABASE_URL=postgresql://username:password@host:port/database

# --------------------------------------------
# SESSION CONFIGURATION
# --------------------------------------------
SESSION_SECRET=your_secure_session_secret

# --------------------------------------------
# AI SERVICES
# --------------------------------------------
# OpenAI API Key
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic Claude API Key
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Perplexity API Key (for trend discovery)
PERPLEXITY_API_KEY=your_perplexity_api_key

# --------------------------------------------
# BILLING & PAYMENTS (Production Only)
# --------------------------------------------
# Stripe Secret Key (live key in prod, test key in dev)
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key

# Stripe Publishable Key (for frontend)
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key

# Stripe Webhook Secret (for webhook signature verification)
STRIPE_WEBHOOK_SECRET=whsec_your_webhook_secret

# Stripe Price IDs for subscription tiers
STRIPE_PRICE_ID_PRO=price_your_pro_monthly_price_id

# --------------------------------------------
# REPLIT AUTH (Production Only)
# --------------------------------------------
# Replit Auth is automatically configured in production
# Headers provided by Replit:
# - X-Replit-User-Id
# - X-Replit-User-Name  
# - X-Replit-User-Email
# - X-Replit-User-Profile-Image
# No additional configuration needed

# --------------------------------------------
# INTEGRATIONS
# --------------------------------------------
# Make.com Webhook Integration
MAKE_WEBHOOK_URL=https://hook.us2.make.com/your_webhook_url

# Email Service Configuration (CookAIng Marketing Engine)
# Primary: Brevo (Sendinblue)
BREVO_API_KEY=your_brevo_api_key_here

# Fallback: Resend
RESEND_API_KEY=your_resend_api_key_here

# Email Configuration
DEFAULT_FROM_EMAIL=noreply@yourdomain.com
DEFAULT_FROM_NAME=Your Company Name

# Link Signing Secret (for email tracking)
LINK_SIGNING_SECRET=your_secure_link_signing_secret_here

# Webhook Security (REQUIRED for production)
BREVO_WEBHOOK_SECRET=your_brevo_webhook_secret_from_dashboard

# Social Media Integration (Buffer)
BUFFER_ACCESS_TOKEN=your_buffer_access_token_from_app_page

# Blog Publishing Integration (Notion)
NOTION_INTEGRATION_TOKEN=your_notion_integration_token
NOTION_BLOG_DB_ID=your_notion_database_id_for_blog

# Push Notifications (OneSignal)
ONESIGNAL_APP_ID=your_onesignal_app_id
ONESIGNAL_API_KEY=your_onesignal_rest_api_key

# --------------------------------------------
# ANALYTICS (Optional)
# --------------------------------------------
# Google Analytics Measurement ID
VITE_GA_MEASUREMENT_ID=your_ga_measurement_id

# ============================================
# ENVIRONMENT-SPECIFIC SETUP INSTRUCTIONS
# ============================================

# DEVELOPMENT SETUP:
# 1. Set APP_ENV=development
# 2. Set DEV_USER_ID and DEV_USER_EMAIL
# 3. Set BYPASS_LIMITS=1
# 4. Set DISABLE_BILLING=1
# 5. Auth automatically bypassed, unlimited quotas

# PRODUCTION SETUP:
# 1. Set APP_ENV=production
# 2. Update DATABASE_URL to production database
# 3. Set BYPASS_LIMITS=0
# 4. Set DISABLE_BILLING=0 (if using Stripe)
# 5. Enable Replit Auth in workspace (automatic headers)
# 6. Configure Stripe keys if billing enabled
# 7. Set APP_URL to production domain

# ============================================
# TIER LIMITS (Enforced in Production)
# ============================================
# Free Tier: 10 generations per month
# Pro Tier: 500 generations per month
# Unlimited: Development mode only
